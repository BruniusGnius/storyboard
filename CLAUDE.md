# Claude Code Entry — Storyboard

Use `SKILL.md` as the authoritative workflow. Load only the referenced files required by the current stage.

Preserve IDs and locks from upstream artifacts. Build a temporal video storyboard using `SQxx`, `SCxx`, and `SBxxx`. Every selected panel must receive a complete English `previz_prompt_en` so the user can generate/review/correct images even when no image MCP is connected. Keep provisional camera values distinct from final Technical Shot Plan values.
